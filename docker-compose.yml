nginx:
  build: ./_nginx/
  links:
    - "web:web"
  ports:
    - "80:80"
web:
  build: ./web/
  links:
    - "webdb:mysql"
webdb:
  image: "mysql:5.6"
  environment:
    MYSQL_USER: root
    MYSQL_DATABASE: wordpress
