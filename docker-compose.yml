nginx:
  hostname: nginx
  build: ./_nginx/
  links:
    - web
  ports:
    - "80:80"
web:
  hostname: web
  build: ./web/
  links:
    - webdb
webdb:
  hostname: webdb
  image: "mysql:5.6"
  environment:
    MYSQL_USER: root
    MYSQL_DATABASE: wordpress
